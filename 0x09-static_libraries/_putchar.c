#include "main.h"

/**
 * _putchar - computes the absolute value of an integer.
 * @c: the value to be evaluated
 *
 * Return: 0
 */
int _putchar(char c)
{	
	if (c == 'a')
		return (1);
	
	return (0);
}